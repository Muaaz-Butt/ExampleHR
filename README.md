# ExampleHR
Time-Off Microservice built with NestJS and SQLite to manage employee leave requests while ensuring consistency with an external HCM (source of truth). Supports real-time validation, batch sync, retry handling, and robust testing for reliability in distributed systems.
